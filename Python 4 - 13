# A day at the supermarket
# Lists + Functions
# Python 4/13

def fizz_count(x):
    count = 0
    for item in x:
        if item == 'fizz':
            count += 1
    return count

fizz_count(["fizz", "cat", "fizz"])
