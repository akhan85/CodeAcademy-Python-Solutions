# Lists and Functions
# Modifying each element in a list in a function 13/18

n = [3, 5, 7]

def double_list(x):
    for i in range(0, len(x)):
        x[i] = x[i] *2
    return x

# Don't forget to return your new list!

print double_list(n)
